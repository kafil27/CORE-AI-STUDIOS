import 'package:flutter/material.dart';

class VoiceAIScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Voice AI'),
        centerTitle: true,
      ),
      body: Center(
        child: Text('Voice AI Coming Soon'),
      ),
    );
  }
} 